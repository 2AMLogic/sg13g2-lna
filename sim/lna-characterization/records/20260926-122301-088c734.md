# Record 20260926-122301-088c734

- **Experiment**: lna-characterization (issue #18)
- **Claim**: circuit-level 50 Ohm-port S-parameters (S11/S21/S12/S22),
  noise figure, k-factor/mu-factor stability and two-tone IIP3 of
  `design/lna.sch` (via its committed netlist), across a
  process x temperature x supply PVT grid. **Not** a conformance
  claim against any `spec/target-spec.md` row. That table's
  RATIFIED rows (decision record 0002) are a bar, not a
  hypothesis -- but their *verification* is gated on the #26 bias
  and #27 matching re-runs, so this record is evidence about the
  as-committed DUT, not a pass/fail verdict against the spec.
- **DUT**: `design/netlist/lna.spice` (sha256
  `23f9445b01dbec997a2247e40deb6fcaf09b4c2a4a3301c0aae2d37ad27768d1`), inlined verbatim into every generated
  deck as a `.subckt lna vdd vss rfin rfout`.
- **Bench definitions**: see `README.md` -- short summary: 50 Ohm
  reference impedance at both ports (`sp` analysis port sources);
  NF from an input-referred `.noise` measurement with NOISELESS
  Rs/RL and the source noise re-introduced analytically at
  T0 = 290 K (IEEE) and at 300.15 K, cross-checked against
  ngspice's own two-port `sp` NF; IIP3 from a coherent two-tone
  transient + rectangular-window FFT, no zero padding.
- **PDK**: `ihp-sg13g2` at `/home/ubuntu/share/pdk` -- pinned release: see
  `sim/pdk.json` (IHP-Open-PDK v0.3.0).
- **Device model sections**: HBT from `cornerHBT.lib`
  (`hbt_typ`/`hbt_bcs`/`hbt_wcs`; `sf`/`fs` fall back to
  `hbt_typ` -- no skewed HBT section exists in this PDK), MOS from
  `cornerMOShv.lib` mapped straight through by the five labels
  (`mos_tt`/`mos_ff`/`mos_ss`/`mos_sf`/`mos_fs`), with the
  PSP103.6 OSDI models preloaded (`pre_osdi`). Identical mapping to
  `sim/lna-bias-pvt/run_biasop_sweep.sh`, so both benches describe
  the same 45 cells.
- **ngspice**: `** ngspice-46 : Circuit level simulation program`, with
  `.options gmin=1e-10` in every deck. That is 100x ngspice-46's
  own 1e-12 default and is NOT what the two pre-DR-0003 records ran
  under; the DR-0003 flat-reference core does not converge at the
  coldest/lowest-rail cells without it. Measured cost of the
  override at the nominal cell: every recorded quantity moves in the
  7th significant figure or beyond (largest relative move anywhere
  4.5e-5, on the ill-conditioned Rollett k). See `README.md`
  section "gmin and the DR-0003 core".
- **Ideal-passive assumption (this record's largest stated limit)**:
  `Le`/`Lc` are ideal infinite-Q SPICE `L` primitives and the
  capacitors/resistors are ideal `C`/`R`, because SG13G2's open PDK
  ships no simulatable inductor model (issue #5, upstream
  `2AMLogic/klayout-tools#1519`). Only the HBTs and the DR-0003
  core's MOS devices are real PDK models. This makes every
  gain/NF/S11/S22 number here the MOST OPTIMISTIC case, and makes
  the stability numbers the LEAST-DAMPED case. No number in this
  record is verified against a physical passive model, let alone
  silicon. Full list: `README.md` section "Model limitations".
- **PVT grid**: corner_label {typ,bcs,wcs,sf,fs} x temp
  {-40,27,125} C x VDD {1.62,1.80,1.98} V =
  45 cells, each run for both phases.
- **In-band sweep**: 11 points, 2.4e9 .. 2.4835e9 Hz.
- **Stability sweep**: 1e7 .. 3e10 Hz at 40 points/decade
  (>= 3x the upper band edge, as the stability spec row requires).
- **Two-tone**: f1=2441406250.0 Hz, f2=2449035644.53125 Hz, spacing=7629394.53125 Hz;
  FFT N=65536 samples at 4e-12 s/sample (window 2.6214e-07 s,
  bin 3814697.265625 Hz), rectangular window, no zero padding,
  1e-6 s of transient discarded before the window.
- **Result**: 138 ngspice invocations (run 1 at a
  time; each writes only its own files, so concurrency affects wall-clock
  time only, not any number below).
- **Failed cells**: none.
- **In-band S11 (worst of 45 PVT cells)**: -2.9616 dB at bcs/-40 C/VDD=1.98 V; best cell -3.2227 dB at wcs/125 C/VDD=1.62 V.
- **In-band S22 (worst)**: -0.0033 dB at bcs/-40 C/VDD=1.98 V.
- **In-band S21**: min 10.2748 dB at wcs/125 C/VDD=1.62 V; max 12.3539 dB at bcs/-40 C/VDD=1.98 V.
- **NF (T0 = 290 K, worst in-band point per cell)**: worst 3.7759 dB at wcs/125 C/VDD=1.62 V; best 1.8432 dB at bcs/-40 C/VDD=1.98 V.
- **NFmin (ngspice `sp`, at 2.4 GHz, i.e. what an ideal noise match would give this circuit)**: best 2.0749 dB at bcs/-40 C/VDD=1.98 V.
- **Stability (k, 10 MHz .. 30 GHz)**: worst-case k = -3.530083 at 5.011872e+07 Hz, bcs/125 C/VDD=1.98 V; 45/45 PVT cells have k < 1 somewhere in the swept range (60/140 swept points at the worst cell).
- **Stability (k, in-band only)**: worst-case k = 3.389937 (mu = 1.000404, |Delta| = 0.690292) at wcs/125 C/VDD=1.98 V.
- **Stability (mu, 10 MHz .. 30 GHz)**: worst-case mu = 0.999996 at 5.956621e+08 Hz, bcs/-40 C/VDD=1.62 V.
- **Negative-resistance check (|S11|/|S22| > 1 with the other port at 50 Ohm)**: max |S11| = 0.817548 at 1.000000e+07 Hz (bcs/125 C/VDD=1.62 V); max |S22| = 1.000000 at 1.584893e+08 Hz (bcs/-40 C/VDD=1.62 V).
- **IIP3 (two-tone, 93 drive points)**: -1.4171 dBm at the nominal cell (typ/27 C/VDD=1.80 V, 2 mV peak/tone drive); across all points min -2.3333 dBm at wcs/-40 C/VDD=1.62 V, max 0.4979 dBm at bcs/125 C/VDD=1.98 V.
- **IM3 slope check (two drive levels per cell)**: slope in [2.988, 3.001] (ideal cubic = 3.000) -> the extrapolation's 3:1 assumption holds at every cell.
- **Bias across PVT**: I_C1 spans 3.657 .. 4.151 mA; P_dc spans 7.187 .. 9.768 mW (worst at bcs/125 C/VDD=1.98 V).
- **Links**:
  - Templates: `testbench/tb_lna_sparam.spice.tmpl`,
    `testbench/tb_lna_iip3.spice.tmpl`
  - Parser: `parse_lna_sweep.py`
  - Per-point generated netlists: `netlist-snapshots/20260926-122301-088c734/`
  - Per-point raw ngspice logs and wrdata tables:
    `corners/20260926-122301-088c734/`
  - Per-frequency in-band S-parameter/NF/stability CSV:
    `records/20260926-122301-088c734-sparam.csv`
  - Per-point IIP3 CSV: `records/20260926-122301-088c734-iip3.csv`
  - Per-cell summary CSV: `records/20260926-122301-088c734-summary.csv`
- **Timestamp / author**: 2026-09-26T13:06:53Z, Loom Builder
  (agent), issue #18.
